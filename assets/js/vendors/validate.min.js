// Validación de formulario
const constraints = {
    email: {
        presence: true,
        email: true
    },
    password: {
        presence: true,
        length: {
            minimum: 8
        }
    }
};

const errors = validate(document.getElementById('loginForm'), constraints);
if (errors) {
    showErrors(errors);
}